<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <style>
                html {
                    height: 100%;
                }

                body {
                    margin: 0;
                    height: 100%;
                }

                #outer-container {
                    display: table;
                    width: 100%;
                    height: 100%;
                }

                #sidebar {
                    background-color: #c5dece;
                    display: table-cell;
                    width: 15%;
                    vertical-align: top;
                    padding-left: 15px;
                    padding-top: 15px;
                }

                #content {
                    background-color: azure;
                    display: table-cell;
                    width: 85%;
                    vertical-align: top;
                    padding-left: 15px;
                    padding-top: 15px;
                }

                ul.sidebar {
                    list-style-type: none;
                }

                #content #result.search-container {
                    width: 100%;
                    background-color: #f9f9f9;
                    padding: 20px;
                    box-shadow: 0px 2px 5px rgba(0, 0, 0, 0.1);
                    position: sticky;
                    top: 0;
                    z-index: 1000;
                }

                .search-container input[type="text"] {
                    width: 80%;
                    padding: 10px;
                    font-size: 16px;
                    border: 1px solid #ccc;
                    border-radius: 5px;
                }

                #content #result.results-container {
                    margin: 20px;
                    padding-top: 20px;
                    width: 90%;
                }

                table {
                    width: 100%;
                    border-collapse: collapse;
                }

                table th, table td {
                    border: 1px solid #ddd;
                    padding: 10px;
                    text-align: left;
                }

                table th {
                    background-color: #f2f2f2;
                }

                table tr:nth-child(even) {
                    background-color: #f9f9f9;
                }

                table tr:hover {
                    background-color: #f1f1f1;
                }
            </style>
        {% endblock %}
        {% block javascript %}
        {% endblock %}
    </head>
    <body>
        <div id="outer-container">
            <div id="sidebar">{% block sidebar %}{% endblock %}</div>
            <div id="content">{% block content %}{% endblock %}</div>
        </div>
    </body>
</html>
